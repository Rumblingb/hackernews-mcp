startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        description: "API key for premium features (optional for free tier)"
  commandFunction: |
    (config) => ({command: 'python3', args: ['server.py']})
